# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트가 공통으로 따라야 할 안내서다. `frontend/`를 수정할 때는 이 문서와 [프론트엔드 작업 규칙](./frontend/AGENTS.md)을 함께 따른다.

## 작업 원칙

- 기본 작업 범위는 `frontend/`와 그 동작을 설명하는 `docs/`다.
- 사용자가 명시적으로 요청하기 전에는 `backend/` 코드를 열거나 수정하지 않는다.
- 현재 우선순위는 백엔드 연동보다 공연사 관리자용 UI와 지원자 흐름을 완성하는 것이다.
- 코드를 작성하기 전에 관련 도메인·흐름·API 문서와 현재 구현을 확인한다.
- 사용자 흐름이나 계약이 바뀌지 않는 내부 리팩터링에는 불필요한 문서 변경을 만들지 않는다.
- 문서의 **결정된 설계**, **현재 구현**, **추가 결정 필요**를 혼동하지 않는다.

## 저장소 준비와 Git

- 루트 `node_modules/`가 없으면 루트에서 `npm install`을 실행해 Husky hook을 설치한다.
- `--no-verify`와 `HUSKY=0`으로 hook을 우회하지 않는다.
- 백엔드 Checkstyle은 `npm run checkstyle`로 수동 실행할 수 있다.
- push 전 현재 통합 브랜치인 `origin/main`을 fetch하고 현재 브랜치를 그 위로 rebase한다. 충돌을 해결하고 검증한 뒤 push한다.
- 통합 브랜치가 바뀌면 `origin/main`을 고정 규칙으로 가정하지 말고 Git 컨벤션과 결정 기록을 먼저 갱신한다.

## 프로젝트 개요

예술IN은 공연 공고로 유입된 지원자의 접수와 심사를 관리하는 서비스다.

```text
2026-yesulin/
├── backend/    Spring Boot 애플리케이션
├── frontend/   공연사 관리자·지원자 화면을 제공하는 Next.js 애플리케이션
└── docs/       도메인, 비즈니스 흐름, 컨벤션, 정책과 결정 기록
```

지원자 공개 화면은 `/apply/{postingId}`에서 목 데이터로 동작한다. 공개 지원서는 아직 실제 서버 저장이나 공연사 전달을 하지 않으며 프론트엔드 상태만 검증한다. 최신 목표 정책과 현재 구현의 차이는 `docs/domain-design.md`에서 관리한다.

## 먼저 읽을 문서

- `README.md`: 제품 범위, 실행 방법과 코드 지도
- `docs/domain-design.md`: 최신 도메인 규칙, 현재 구현 차이와 추가 결정 질문
- `docs/flowchart/actor.mmd`, `docs/flowchart/producer.mmd`: 사용자별 비즈니스 흐름
- `docs/convention/api-convention.md`: 백엔드 목표 API와 현재 프런트 이관 상태
- `docs/decisions/README.md`: 시각 기반 결정 목록. `agent-required` 기록은 반드시 읽는다.
- `docs/backend/file-upload.md`: 파일 모델, 소유권과 presigned upload 생명주기
- `docs/README.md`: 구현과 문서를 함께 유지하는 기준
- `docs/convention/git-convention.md`: 브랜치, 커밋, push 규칙
- `docs/convention/be-code-convention.md`: 백엔드 코드와 Checkstyle 규칙

문서가 언급하는 `docs/index.html` 프로토타입은 현재 저장소에 없으므로 기준 파일로 가정하지 않는다. 화면 의도는 살아 있는 명세와 현재 구현을 우선해 판단하고, 모호하면 사용자에게 확인한다.

## 도메인 규칙

```text
Performance 공연
  └ Posting 공고
      ├ Role 배역 하나 이상
      └ Application 지원서 ── Role 하나 이상 선택
```

- 별도의 자유 배역 타입은 없다. 공연사가 일반 배역 하나를 `자유`라는 이름으로 등록한다.
- 공고의 배역이 하나면 지원자의 배역 선택 화면을 생략한다.
- 동일 계정은 같은 공고에 지원서를 하나만 제출할 수 있다. 복수 배역 선택은 중복 지원이 아니다.
- 전형은 배역 단위로 독립 진행하며 차수 상태는 `(배역, 차수)`에 속한다.
- 심사 결과는 `(지원서, 배역, 차수)`에 속하며 다른 배역이나 차수의 결과를 덮어쓰지 않는다.
- 복수 배역 지원서는 선택한 각 배역의 심사 목록에 별도 대상으로 표시한다.
- 1차 결과에는 `불참`이 없고, 2·3차에서는 선택할 수 있다.
- 이전 차수를 마감해야 다음 차수 합격자 풀이 열린다.
- 검토 대기가 남아 있거나 대상자가 없으면 차수를 마감할 수 없다.
- 배역 조건과 다른 지원자는 차단하지 않고 `mismatchReasons`로 표시한다.
- 관리자 화면은 검색 엔진에 노출하지 않는다.

## 지원서·Draft·프로필 규칙

- 로그인 전·후 작성 내용과 사진은 IndexedDB에 먼저 저장하고 서버 Draft와 지연·묶음 동기화한다.
- 로그인 또는 회원가입 완료 후 현재 브라우저 Draft를 계정에 자동 연결한다.
- 같은 공고의 Draft가 겹치면 최신 수정본 전체가 이전 Draft를 교체한다.
- 최종 제출 전 Draft는 공연사에 공개하지 않는다.
- 최종 제출은 인증된 계정만 할 수 있고 서버 시각 기준 모집 마감 이후에는 거부한다.
- 제출 지원서는 프로필·사진·답변·선택 배역의 불변 스냅샷이다.
- 프로필 저장은 하나의 선택 동의로 처리하며 제출 화면에서 명시적으로 선택할 때만 지원서 수정값을 프로필에도 반영한다.
- 커스텀 답변은 재사용 프로필에 저장하지 않는다.
- 개인 사진 보관함에서 사진을 삭제해도 Draft와 제출 지원서는 독립적으로 유지한다.

## API와 구현 차이

- 백엔드 목표 경로는 `docs/convention/api-convention.md`의 `/api/v1/**` 계약을 따른다.
- 현재 프런트·MSW 경로와 목표 계약의 차이는 API 문서의 이관 상태에서 관리한다.
- 실제 계약을 바꾸는 작업은 타입, API 호출, MSW 핸들러와 관련 문서를 함께 맞춘다.
- 현재 프론트 구현이 목표 정책과 다르면 코드를 현재 정본으로 확대 해석하지 않는다. `docs/domain-design.md`의 구현 차이를 확인하고 사용자가 구현을 요청한 범위에서만 수정한다.

## 문서 동기화

다음 변경은 구현과 같은 작업 단위에서 관련 문서도 갱신한다.

- 사용자 흐름 또는 비즈니스 규칙
- 화면 경로와 API 요청·응답 구조
- 권한, 상태 전이와 데이터 생명주기
- MSW와 실제 백엔드가 공유해야 할 계약
- 환경 변수, 실행 명령과 폴더 구조
- 팀이 알아야 할 현재 한계나 의사결정

공통 결정은 `docs/decisions/`에 UTC 시각 기반 파일명으로 250단어 이내 기록한다. 중요한 결정은 `agent-required: true`로 표시하고 결정 목록에 연결한다. `CLAUDE.md`는 이 문서를 불러오므로 같은 규칙이 적용된다. 사용자가 구현을 요청한 변경에서 새 문서·구현 차이가 생기면 작업이 완료된 것으로 보지 않는다. 아직 구현하지 않은 목표 정책은 `docs/domain-design.md`의 현재 구현 차이에 명시한다.
