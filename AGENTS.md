# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트의 공통 규칙이다. `frontend/`를 수정할 때는 [프론트엔드 작업 규칙](./frontend/AGENTS.md)도 함께 따른다.

## 작업 범위와 원칙

- 기본 작업 범위는 `frontend/`와 그 동작을 설명하는 `docs/`다.
- 사용자가 명시적으로 요청하기 전에는 `backend/` 코드를 열거나 수정하지 않는다.
- 현재 우선순위는 백엔드 연동보다 공연사 관리자 UI와 지원자 흐름을 완성하는 것이다.
- 변경 전에 관련 코드와 문서를 확인하고 **결정된 설계**, **현재 구현**, **추가 결정 필요**를 혼동하지 않는다.
- 사용자 흐름이나 계약이 바뀌지 않는 내부 리팩터링에는 불필요한 문서 변경을 만들지 않는다.

## 문서 라우팅

모든 작업에서 먼저 `README.md`와 `docs/README.md`를 확인한다. 그 뒤 [문서 라우터](./docs/README.md)의 표에서 현재 작업에 필요한 문서만 고른다.

- 도메인·사용자 흐름·Draft·권한·상태 전이·데이터 생명주기를 바꾸거나 구현할 때만 `docs/domain-design.md`를 읽는다.
- API 또는 MSW 공유 계약을 바꿀 때는 `docs/convention/api-convention.md`와 해당 개발 상세 문서를 읽는다.
- UI·반응형·접근성, MSW 시나리오, 사용자 흐름은 `docs/development/`의 해당 문서만 읽는다.
- 현재 설계의 이유가 필요할 때만 `docs/decisions/README.md`에서 관련 결정을 찾는다.
- `docs/development/`나 `docs/decisions/`를 일괄 로드하지 않고, Git에 없는 내부 Notion·Archive 내용을 임의로 가정하지 않는다.

문서가 언급하는 `docs/index.html` 프로토타입은 현재 저장소에 없으므로 기준 파일로 가정하지 않는다. 화면 의도는 살아 있는 명세와 현재 구현을 우선하고 모호하면 사용자에게 확인한다.

## 저장소 준비와 Git

- 작업을 시작하기 전 GitHub 이슈를 먼저 만들고, 생성과 동시에 프로젝트 보드에 올리며 만든 사람에게 배정한다: `gh issue create --project "2026-yesulin" --label "type:feat" --assignee @me ...`. `@me`는 `gh auth`로 로그인한 계정이므로 각자 실행하면 각자에게 배정된다.
- 이슈 제목은 커밋과 같은 `{type}: {summary}` 형식을 쓰고 `type:*` 라벨을 최소 하나 붙인다. 라벨 목록과 보드 상태 규칙은 [Git 컨벤션](./docs/convention/git-convention.md)에 있다.
- 이미 있는 이슈를 맡을 때도 `gh issue edit 123 --add-assignee @me`로 배정한다. 보드 상태를 `In Progress`로 옮기는 것은 자동화되어 있지 않으므로 사람이 옮긴다.
- PR 본문에 `Closes #123`을 적어 이슈와 연결한다. 병합하면 이슈가 닫히고 보드가 `Done`으로 이동한다.
- 루트 `node_modules/`가 없으면 루트에서 `npm install`을 실행해 Husky hook을 설치한다.
- `--no-verify`와 `HUSKY=0`으로 hook을 우회하지 않는다.
- 백엔드 Checkstyle은 `npm run checkstyle`로 수동 실행할 수 있다.
- push 전 현재 통합 브랜치인 `origin/main`을 fetch하고 현재 브랜치를 그 위로 rebase한다. 충돌을 해결하고 검증한 뒤 push한다.
- 통합 브랜치가 바뀌면 `origin/main`을 고정 규칙으로 가정하지 말고 Git 컨벤션과 현재 설정을 먼저 갱신한다.

## 구현·검증·문서 동기화

- 실제 계약을 바꾸는 작업은 타입, API 호출, MSW 핸들러와 관련 정본을 같이 맞춘다.
- 현재 프론트 구현이 목표 정책과 다르면 코드를 정본으로 확대 해석하지 않는다.
- 사용자 흐름, 비즈니스 규칙, API 필드 의미, 권한·상태 전이·데이터 생명주기, MSW와 백엔드의 공유 계약, 환경 변수·실행 명령이 바뀌면 관련 정본을 구현과 함께 갱신한다.
- 새 API 경계, 권한·소유권, 데이터 생명주기처럼 여러 영역에 영향을 주고 현재 구현에 필요한 결정만 `docs/decisions/`에 250단어 이내로 기록한다.
- 결정 결과가 정본과 코드에 흡수되면 Git에서 제거하고 접근 제한된 Notion Archive로 옮긴다. UI 문구·간격 조정, 정해진 규칙의 구현과 내부 리팩터링에는 결정 파일을 만들지 않는다.
- 사용자가 구현을 요청한 변경에서 새 문서·구현 차이가 생기면 작업이 완료된 것으로 보지 않는다.

`CLAUDE.md`는 이 문서를 불러오므로 같은 규칙이 적용된다.
