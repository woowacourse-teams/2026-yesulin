---
name: 예술in
slug: yesulin
category: performing-arts-audition-platform
last_updated: "2026-08-05"
status: active
language: ko
reference:
  - Toss Design System의 정보 위계, 단일 Primary Action, 시맨틱 토큰, 명확한 상태 표현
  - 공연예술 오디션 지원자 및 공연사 담당자 인터뷰에서 확인한 업무 흐름
---

# 예술in Design System

> 예술in은 뮤지컬·연극 등 공연예술 분야의 오디션 공고 탐색, 지원 자료 재사용, 지원서 제출, 지원자 검토 및 선발을 하나의 흐름으로 연결하는 서비스다.
>
> 이 문서는 예술in의 UI를 구현하거나 수정할 때 사용하는 최우선 디자인 기준이다. 토스의 디자인 원칙을 참고하지만 토스의 브랜드, 금융 서비스 구조, 화면 구성, 컴포넌트를 그대로 복제하지 않는다.

---

## 0. 문서 사용 원칙

### 확정된 요구사항

- 주조색은 **푸른색, 검정색, 흰색**을 사용한다.
- 전체 인상은 **심플하고 직관적이며 신뢰할 수 있는 업무형 서비스**에 가깝게 설계한다.
- 글래스모피즘은 예술in의 시각적 특징으로 사용한다.
- 지원자 화면과 공연사 담당자 화면의 목적과 정보 밀도를 구분한다.
- 기존 기능은 명시적인 요청 없이 제거하거나 다른 기능으로 대체하지 않는다.
- 사용성, 정보 전달, 실수 예방을 시각적 화려함보다 우선한다.

### 디자이너 제안

- 글래스모피즘은 모든 카드에 적용하지 않고, **헤더·필터 바·플로팅 패널·강조 영역**에 제한적으로 사용한다.
- Primary 버튼은 글래스 스타일보다 **불투명한 브랜드 블루**를 사용해 행동을 명확히 한다.
- 서비스 전체를 다크 모드로 만들기보다, 검정색은 헤더·사이드바·히어로 영역과 핵심 텍스트에 사용한다.
- 본문 작업 영역은 흰색과 옅은 회색을 중심으로 구성해 장시간 사용 시 피로를 줄인다.

### 구현 우선순위

충돌이 발생하면 다음 순서로 판단한다.

1. 사용자가 목표를 완료할 수 있는가
2. 제출 및 검토 실수를 예방할 수 있는가
3. 정보 우선순위가 명확한가
4. 접근성과 가독성이 확보되는가
5. 브랜드 일관성이 유지되는가
6. 시각적 장식이 아름다운가

---

## 1. Product Context

### 서비스 목표

예술in은 공연마다 달라지는 지원 양식과 자료 제출 방식을 통합해 다음 문제를 해결한다.

- 지원자가 프로필, 경력, 사진, 노래·연기 영상을 반복해서 작성하고 찾는 문제
- 어떤 자료를 제출했는지 확인하기 어려운 문제
- 공연사 담당자가 서로 다른 형식의 지원서를 수동으로 분류하는 문제
- 지원자의 조건, 경력, 사진, 영상을 여러 파일과 링크에서 반복 확인하는 문제
- 지원서 누락, 잘못된 파일 선택, 검토 상태 누락이 발생하는 문제

### 주요 사용자

#### 지원자

뮤지컬 배우, 연극 배우, 앙상블 배우, 아역 배우 등 오디션에 지원하는 사용자다.

주요 목표:

- 지원 가능한 공고를 빠르게 찾는다.
- 공연명, 배역, 마감일, 일정, 지원 자격을 비교한다.
- 기존에 등록한 프로필과 자료를 재사용한다.
- 현재 어떤 자료가 제출되는지 확인한다.
- 누락 없이 지원서를 제출한다.
- 지원 상태와 다음 일정을 확인한다.

#### 공연사 담당자

제작사, 극단, 기획사, 창작집단 등에서 공고를 등록하고 지원자를 검토하는 사용자다.

주요 목표:

- 공연과 배역별로 공고 및 지원 조건을 설정한다.
- 많은 지원자를 빠르게 검색하고 분류한다.
- 지원자의 핵심 정보와 자료를 한 화면에서 검토한다.
- 평가, 내부 메모, 검토 상태를 관리한다.
- 여러 지원자의 상태를 일괄 변경한다.
- 합격, 보류, 불합격 결과와 변경 이력을 관리한다.

---

## 2. Brand & Visual Concept

### 브랜드 핵심 문장

**예술의 문을 열다.**

예술in의 인터페이스는 사용자가 새로운 무대와 기회로 들어가는 과정을 방해하지 않고 안내해야 한다.

### 시각 키워드

- Clear
- Trustworthy
- Calm
- Professional
- Progressive
- Creative but restrained
- Digital stage entrance

### 시각적 해석

- **Blue**: 기회, 신뢰, 진행, 선택, 디지털 서비스
- **Black**: 전문성, 집중, 무대의 깊이, 정보의 기준점
- **White**: 명료함, 여백, 콘텐츠의 가독성
- **Glass**: 새로운 기회로 이어지는 레이어, 정보의 연결, 현대적인 디지털 경험

### 피해야 할 공연 클리셰

- 커튼, 티켓, 가면, 스포트라이트, 금색 장식의 과도한 사용
- 검정·빨강·금색 조합만으로 공연 분위기를 표현하는 방식
- 공연 포스터처럼 이미지와 장식이 정보를 압도하는 화면
- 모든 카드에 유리 효과를 적용해 경계와 위계가 흐려지는 디자인
- 지나친 네온, 빛 번짐, 3D 오브젝트, 미래주의 장식

---

## 3. Core UX Principles

### 3.1 한 화면에는 하나의 Primary Action만 둔다

각 화면에서 가장 중요한 행동 하나를 브랜드 블루로 강조한다.

예시:

- 공고 상세: `지원하기`
- 지원서 작성: `다음 단계`
- 최종 확인: `지원서 제출`
- 공고 작성: `공고 등록`
- 지원자 상세: `검토 상태 변경`

같은 행동으로 이동하는 Primary 버튼을 여러 위치에 반복하지 않는다.

### 3.2 사용자가 다음에 해야 할 행동을 보여준다

화면을 본 사용자가 별도의 설명 없이 다음을 판단할 수 있어야 한다.

- 지금 어디에 있는가
- 무엇을 확인해야 하는가
- 무엇이 완료되었는가
- 무엇이 누락되었는가
- 다음에 어떤 버튼을 눌러야 하는가

### 3.3 지원 판단에 필요한 정보를 먼저 보여준다

공고 목록 우선순위:

1. 공연명
2. 모집 배역
3. 지원 마감일
4. 연습 및 공연 일정
5. 지원 자격
6. 제출 자료
7. 공연사
8. 공연 이미지

지원자 목록 우선순위:

1. 프로필 사진과 이름
2. 지원 배역
3. 주요 조건
4. 핵심 경력
5. 지원일
6. 현재 검토 상태
7. 메모 여부

### 3.4 반복 입력과 반복 탐색을 줄인다

- 기존 프로필과 자료를 기본 선택지로 제공한다.
- 선택된 자료는 썸네일, 제목, 등록일, 사용 목적과 함께 표시한다.
- 오래된 자료 또는 다른 공연용 자료는 경고한다.
- 신규 등록과 기존 자료 선택을 시각적으로 구분한다.

### 3.5 오류가 발생하기 전에 예방한다

- 필수 항목을 입력 전에 표시한다.
- 파일 형식과 용량을 업로드 전에 안내한다.
- 영상 링크 유효성을 입력 중 확인한다.
- 자동 저장과 마지막 저장 시간을 표시한다.
- 마감된 공고는 지원 버튼을 비활성화한다.
- 제출 전에 전체 미리보기와 누락 요약을 제공한다.
- 제출 이후 수정 가능 여부를 제출 버튼 근처에 표시한다.

### 3.6 상태는 색상만으로 표현하지 않는다

상태는 항상 다음 요소 중 두 가지 이상으로 표현한다.

- 텍스트
- 아이콘
- 배지 모양
- 색상
- 보조 설명

### 3.7 지원자와 공연사 화면의 밀도를 구분한다

- 지원자 화면은 안내와 단계별 행동을 우선한다.
- 공연사 화면은 비교, 검색, 필터, 빠른 상태 변경을 우선한다.
- 공연사 데스크톱 화면을 모바일 카드 화면처럼 과도하게 여유롭게 만들지 않는다.

---

## 4. Color System

### 4.1 Base Palette

```css
:root {
  /* Brand Blue */
  --blue-50:  #eef5ff;
  --blue-100: #dceaff;
  --blue-200: #b8d5ff;
  --blue-300: #86b6ff;
  --blue-400: #4f94ff;
  --blue-500: #246bfe;
  --blue-600: #1d5ce5;
  --blue-700: #194fc4;
  --blue-800: #183f92;
  --blue-900: #172f65;

  /* Ink & Neutral */
  --ink-950:  #080b12;
  --ink-900:  #111722;
  --ink-800:  #1b2432;
  --ink-700:  #334155;
  --gray-600: #475569;
  --gray-500: #64748b;
  --gray-400: #94a3b8;
  --gray-300: #cbd5e1;
  --gray-200: #e2e8f0;
  --gray-100: #f1f5f9;
  --gray-50:  #f8fafc;
  --white:    #ffffff;

  /* Semantic */
  --green-600:  #16a34a;
  --green-50:   #f0fdf4;
  --orange-600: #d97706;
  --orange-50:  #fff7ed;
  --red-600:    #dc2626;
  --red-50:     #fef2f2;
}
```

### 4.2 Semantic Tokens

```css
:root {
  /* Background */
  --bg-canvas: #f7f9fc;
  --bg-primary: var(--white);
  --bg-secondary: var(--gray-50);
  --bg-dark: #0b1018;

  /* Surface */
  --surface-solid: var(--white);
  --surface-muted: var(--gray-50);
  --surface-glass: rgba(255, 255, 255, 0.72);
  --surface-glass-strong: rgba(255, 255, 255, 0.86);
  --surface-glass-dark: rgba(8, 11, 18, 0.72);

  /* Text */
  --text-primary: var(--ink-900);
  --text-secondary: var(--gray-600);
  --text-tertiary: var(--gray-500);
  --text-placeholder: var(--gray-400);
  --text-inverse: var(--white);
  --text-brand: var(--blue-600);
  --text-danger: var(--red-600);

  /* Action */
  --action-primary: var(--blue-500);
  --action-primary-hover: var(--blue-600);
  --action-primary-pressed: var(--blue-700);
  --action-secondary: var(--gray-100);
  --action-disabled: var(--gray-200);

  /* Border */
  --border-default: var(--gray-200);
  --border-strong: var(--gray-300);
  --border-focus: var(--blue-500);
  --border-glass: rgba(255, 255, 255, 0.64);
  --border-glass-dark: rgba(255, 255, 255, 0.14);

  /* Overlay */
  --overlay-scrim: rgba(8, 11, 18, 0.56);
  --overlay-press: rgba(8, 11, 18, 0.10);
}
```

### 4.3 사용 비율

권장 비율:

- White / Light Neutral: 65~75%
- Black / Dark Ink: 15~25%
- Brand Blue: 5~10%
- Semantic colors: 필요한 상태에만 사용

블루는 화면의 모든 요소를 꾸미는 색이 아니라 **선택, 진행, 핵심 행동**을 나타내는 색이다.

### 4.4 검정색 사용 규칙

검정색은 다음 영역에 사용한다.

- 데스크톱 공연사 사이드바
- 랜딩페이지 또는 로그인 화면의 히어로 영역
- 주요 제목과 본문 텍스트
- 영상 플레이어 주변 배경
- 데이터 검토 화면의 기준이 되는 강한 텍스트

검정 배경을 전체 본문 영역에 적용하지 않는다. 지원서 작성과 지원자 검토는 밝은 배경에서 진행한다.

### 4.5 상태 색상

| 상태 | 배경 | 텍스트/아이콘 | 라벨 예시 |
|---|---|---|---|
| 작성 중 | gray-100 | gray-600 | 작성 중 |
| 제출 완료 | blue-50 | blue-700 | 제출 완료 |
| 검토 중 | blue-50 | blue-700 | 검토 중 |
| 추가 자료 요청 | orange-50 | orange-600 | 추가 자료 요청 |
| 오디션 대상 | blue-100 | blue-800 | 오디션 대상 |
| 보류 | gray-100 | ink-700 | 보류 |
| 합격 | green-50 | green-600 | 합격 |
| 불합격 | red-50 | red-600 | 불합격 |

`합격`, `불합격`은 색상과 텍스트를 함께 사용하고 아이콘을 추가할 수 있다.

### 4.6 대비 규칙

- 일반 본문은 WCAG AA 기준 4.5:1 이상을 목표로 한다.
- 큰 글자와 아이콘은 3:1 이상을 확보한다.
- 글래스 표면 위 텍스트는 배경 이미지와 관계없이 읽혀야 한다.
- 대비를 보장할 수 없는 경우 글래스 투명도를 높이거나 불투명 표면으로 전환한다.

---

## 5. Glassmorphism System

### 5.1 기본 원칙

글래스모피즘은 예술in의 시각적 정체성이지만 정보 구조를 흐리게 만들면 안 된다.

사용 가능한 영역:

- 상단 글로벌 헤더
- 검색 및 필터 바
- 랜딩페이지의 핵심 정보 패널
- 데스크톱 지원자 상세 플로팅 패널
- 모바일 하단 고정 액션 영역
- 모달과 바텀시트의 상단 표면
- 선택된 프로필 또는 미디어 미리보기

사용하지 않는 영역:

- 모든 공고 카드
- 긴 지원서 입력 폼 전체
- 데이터가 많은 테이블의 각 행
- 오류 메시지와 필수 안내
- 파일 업로드 진행률
- 배지와 작은 컨트롤

### 5.2 Glass Tokens

```css
.glass-surface {
  background: var(--surface-glass);
  border: 1px solid var(--border-glass);
  box-shadow:
    0 12px 32px rgba(15, 23, 42, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.48);
  backdrop-filter: blur(18px) saturate(130%);
  -webkit-backdrop-filter: blur(18px) saturate(130%);
}

.glass-surface-strong {
  background: var(--surface-glass-strong);
  border: 1px solid rgba(255, 255, 255, 0.78);
  box-shadow: 0 16px 40px rgba(15, 23, 42, 0.12);
  backdrop-filter: blur(24px) saturate(140%);
  -webkit-backdrop-filter: blur(24px) saturate(140%);
}

.glass-surface-dark {
  background: var(--surface-glass-dark);
  border: 1px solid var(--border-glass-dark);
  box-shadow: 0 16px 40px rgba(0, 0, 0, 0.24);
  backdrop-filter: blur(20px) saturate(120%);
  -webkit-backdrop-filter: blur(20px) saturate(120%);
}

@supports not (backdrop-filter: blur(1px)) {
  .glass-surface,
  .glass-surface-strong {
    background: rgba(255, 255, 255, 0.96);
  }

  .glass-surface-dark {
    background: rgba(8, 11, 18, 0.94);
  }
}
```

### 5.3 제한 규칙

- 한 화면의 같은 깊이에서 글래스 레이어를 2개 이상 중첩하지 않는다.
- 글래스 카드 내부에 또 다른 글래스 카드를 넣지 않는다.
- blur는 24px을 초과하지 않는다.
- 투명도는 본문 가독성을 해치지 않는 범위에서만 사용한다.
- Primary 버튼은 글래스가 아니라 불투명 브랜드 블루를 사용한다.
- 저사양 환경과 `prefers-reduced-transparency`를 고려해 불투명 대체 스타일을 제공한다.

---

## 6. Typography

### 기본 서체

```css
font-family:
  "Pretendard Variable",
  Pretendard,
  -apple-system,
  BlinkMacSystemFont,
  "Apple SD Gothic Neo",
  "Noto Sans KR",
  "Segoe UI",
  sans-serif;
```

한글 본문 가독성, 숫자 정렬, 다양한 웨이트를 고려해 Pretendard Variable을 기본으로 사용한다.

### Type Scale

```yaml
display:  { size: 48px, line-height: 1.18, weight: 700, tracking: -0.03em }
h1:       { size: 36px, line-height: 1.25, weight: 700, tracking: -0.025em }
h2:       { size: 28px, line-height: 1.30, weight: 700, tracking: -0.02em }
h3:       { size: 22px, line-height: 1.35, weight: 700, tracking: -0.015em }
title-1:  { size: 18px, line-height: 1.45, weight: 600, tracking: -0.01em }
title-2:  { size: 16px, line-height: 1.45, weight: 600, tracking: -0.005em }
body-1:   { size: 16px, line-height: 1.60, weight: 400, tracking: -0.005em }
body-2:   { size: 14px, line-height: 1.55, weight: 400, tracking: 0 }
label:    { size: 14px, line-height: 1.35, weight: 600, tracking: 0 }
caption:  { size: 12px, line-height: 1.45, weight: 500, tracking: 0 }
```

### 사용 원칙

- 데스크톱 본문 기본 크기는 16px, 데이터 밀도가 높은 표·보조 정보는 14px을 사용한다.
- 모바일 본문은 16px 미만으로 줄이지 않는다.
- 공고 마감일, 지원일, 나이, 키, 평가 점수처럼 비교가 필요한 숫자는 `tabular-nums`를 사용한다.
- 제목은 짧고 명확하게 작성하며 한 화면에서 3단계 이상의 제목 위계를 만들지 않는다.
- 중요한 상태를 굵은 글씨만으로 전달하지 않는다.

```css
.numeric-data {
  font-variant-numeric: tabular-nums;
}
```

---

## 7. Spacing, Radius & Elevation

### Spacing

4px 단위의 간격 체계를 사용한다.

```yaml
space-1: 4px
space-2: 8px
space-3: 12px
space-4: 16px
space-5: 20px
space-6: 24px
space-8: 32px
space-10: 40px
space-12: 48px
space-16: 64px
space-20: 80px
```

권장 사용:

- 라벨과 입력창: 8px
- 밀접한 입력 항목: 16px
- 폼 그룹 사이: 32px
- 카드 내부 여백: 20~24px
- 섹션 사이: 48~64px
- 모바일 화면 좌우 여백: 20px
- 데스크톱 콘텐츠 좌우 여백: 32~48px

### Radius

```yaml
radius-xs: 6px
radius-s: 8px
radius-m: 12px
radius-l: 16px
radius-xl: 20px
radius-2xl: 24px
radius-full: 999px
```

사용 규칙:

- 입력창: 12px
- 버튼: 12~14px
- 일반 카드: 16px
- 글래스 패널: 20~24px
- 모달 및 바텀시트: 20~24px
- 배지와 칩: full 또는 8px

모든 요소를 과도하게 둥글게 만들지 않는다. 데이터 테이블과 밀도 높은 업무 화면은 8~12px을 사용한다.

### Elevation

```css
:root {
  --shadow-1: 0 2px 8px rgba(15, 23, 42, 0.06);
  --shadow-2: 0 8px 24px rgba(15, 23, 42, 0.08);
  --shadow-3: 0 16px 40px rgba(15, 23, 42, 0.12);
  --shadow-modal: 0 24px 64px rgba(8, 11, 18, 0.20);
}
```

- 일반 카드에는 그림자를 기본 적용하지 않는다.
- 카드 구분은 여백, 배경색, 1px 보더를 우선한다.
- 그림자는 플로팅 필터, 상세 패널, 드롭다운, 모달처럼 실제 깊이가 필요한 요소에만 사용한다.

---

## 8. Motion

```yaml
duration-fast: 120ms
duration-base: 200ms
duration-slow: 280ms
ease-standard: cubic-bezier(0.22, 0.61, 0.36, 1)
ease-enter: cubic-bezier(0.16, 1, 0.3, 1)
```

### 사용 규칙

- hover, focus, pressed: 120~200ms
- 패널과 바텀시트 진입: 200~280ms
- 상태 변경 후 피드백: 200ms 이내에 시작
- 과도한 바운스, 패럴랙스, 지속적인 빛 번짐 애니메이션을 사용하지 않는다.
- 로딩은 정적인 skeleton 또는 낮은 강도의 pulse를 사용한다.
- `prefers-reduced-motion` 환경에서는 이동 애니메이션을 제거하고 opacity 전환만 사용한다.

---

## 9. Layout System

### Breakpoints

```yaml
mobile: 0~767px
tablet: 768~1199px
desktop: 1200~1599px
wide: 1600px 이상
```

### Global Container

- 지원자 일반 페이지: 최대 1200px
- 지원서 작성 페이지: 최대 880px
- 공연사 관리 화면: 최대 너비 제한 없이 1440~1920px에서 확장
- 랜딩페이지 콘텐츠: 최대 1280px

### Applicant Layout

#### 모바일

- 한 열 구조
- 하단 고정 Primary Action 사용 가능
- 공고 정보는 이미지보다 텍스트를 먼저 배치
- 필터는 가로 스크롤 칩 또는 바텀시트로 제공
- 지원서 단계별 작성에 최적화

#### 데스크톱

- 공고 목록은 2열 또는 넓은 리스트형 사용
- 공고 상세는 본문 + 우측 지원 요약 패널 구조 사용 가능
- 지원서 작성은 중앙 720~880px 단일 컬럼
- 최종 제출 요약은 우측 sticky 패널 사용 가능

### Company Workspace Layout

공연사 화면은 감성적 포트폴리오가 아니라 업무용 관리 도구로 설계한다.

권장 데스크톱 구조:

```text
┌──────────────┬──────────────────────────────────┬──────────────────┐
│ 공연 사이드바 │ 지원자 목록 / 테이블 / 필터       │ 지원자 상세 패널 │
│ 240~280px    │ min 640px                        │ 380~460px        │
└──────────────┴──────────────────────────────────┴──────────────────┘
```

- 좌측 사이드바는 검정 또는 매우 짙은 남색을 사용한다.
- 중앙 목록은 흰색 기반의 높은 정보 밀도를 유지한다.
- 우측 상세 패널은 필요할 때 열고 닫을 수 있다.
- 상세 패널이 열린 상태에서도 목록의 핵심 열이 사라지지 않게 한다.
- 화면 너비가 부족하면 상세 패널을 오버레이 패널로 전환한다.

### Sticky Areas

다음 요소는 사용 목적에 따라 sticky 처리할 수 있다.

- 지원서 단계 표시기
- 모바일 하단 제출 버튼
- 공연사 검색 및 필터 바
- 지원자 상세의 이름·배역·상태 헤더
- 영상 검토 중 상태 변경 컨트롤

---

## 10. Navigation

### 지원자 글로벌 내비게이션

권장 메뉴:

- 오디션 찾기
- 내 프로필
- 저장한 자료
- 지원 내역

모바일에서는 하단 탭 또는 축약된 메뉴를 사용할 수 있다. 하단 탭은 최대 4개를 권장한다.

### 공연사 글로벌 내비게이션

권장 메뉴:

- 대시보드
- 공연 및 공고
- 지원자 관리
- 메시지 또는 안내
- 설정

공연사 화면의 사이드바는 검정 배경과 흰색 텍스트를 사용하고, 현재 메뉴는 블루 배경 또는 블루 아이콘과 텍스트로 표시한다.

### 현재 위치

- 페이지 제목과 breadcrumb를 함께 사용할 수 있다.
- 지원서 작성에서는 breadcrumb 대신 단계 표시기를 사용한다.
- 현재 메뉴는 색상뿐 아니라 배경, 아이콘, `aria-current`로 표시한다.

---

## 11. Components

### 11.1 Button

#### 종류

- Primary: 가장 중요한 행동
- Secondary: 보조 행동
- Ghost: 낮은 우선순위 행동
- Destructive: 삭제, 취소, 지원 철회
- Text: 부가 링크

#### 크기

| Size | Height | Radius | Label |
|---|---:|---:|---|
| Large | 52px | 14px | 16px / 600 |
| Medium | 44px | 12px | 14px / 600 |
| Small | 36px | 10px | 13px / 600 |

#### 상태

- default
- hover
- pressed
- focus-visible
- loading
- disabled

#### 규칙

- 한 화면에서 Primary 버튼은 하나만 사용한다.
- 버튼 문구는 행동 결과를 직접 말한다.
- 아이콘만 있는 버튼은 44×44px 이상의 hit area를 갖는다.
- Primary 버튼에 글래스 효과를 사용하지 않는다.

좋은 문구:

- `지원하기`
- `다음 단계`
- `지원서 제출`
- `선택한 자료 사용`
- `검토 상태 변경`

피해야 할 문구:

- `확인`
- `진행`
- `여기를 눌러주세요`
- `완료하기`처럼 결과가 불분명한 표현

### 11.2 Text Field

- 기본 높이 48px
- radius 12px
- 기본 배경은 흰색 또는 gray-50
- 기본 보더는 gray-200 1px
- focus는 blue-500 2px 또는 1.5px
- error는 red-600 보더와 구체적인 오류 문구를 함께 표시
- placeholder 외에 항상 label을 제공

상태:

- empty
- filled
- focus
- disabled
- read-only
- error
- success

오류 예시:

- `이름을 입력해 주세요.`
- `지원 가능한 영상 링크인지 확인해 주세요.`
- `PDF, JPG, PNG 파일만 등록할 수 있어요.`
- `파일 크기는 20MB 이하여야 해요.`

### 11.3 Search Field

- 데스크톱 공연사 화면에서 320~420px 너비를 권장한다.
- 검색 대상이 명확하도록 placeholder를 작성한다.

예시:

- `지원자 이름을 검색해 주세요.`
- `공연명 또는 배역을 검색해 주세요.`

검색 결과가 없으면 입력값을 포함한 빈 화면 문구를 표시한다.

### 11.4 Select & Dropdown

- 선택지가 5개 이하이고 주변 맥락이 중요하면 trigger 근처의 dropdown을 사용한다.
- 모바일에서 선택지가 많으면 bottom sheet를 사용한다.
- 현재 선택값을 버튼 라벨에 표시한다.
- 키보드 방향키와 Enter로 조작 가능해야 한다.

### 11.5 Chip & Filter

- Chip은 빠른 필터와 다중 선택에 사용한다.
- 선택되지 않은 chip은 흰색 + gray 보더
- 선택된 chip은 ink-900 배경 + 흰 텍스트 또는 blue-50 + blue-700
- 필터 해제 버튼을 명확히 제공한다.
- 적용된 필터 수를 `필터 3`처럼 표시할 수 있다.

### 11.6 Status Badge

- 높이 24~28px
- 텍스트는 12~13px / 600
- 상태별 시맨틱 색을 사용한다.
- 색상뿐 아니라 상태명을 반드시 표시한다.

### 11.7 Audition Card

공고 카드에서 이미지는 보조 정보다.

필수 구성:

1. 공연명
2. 모집 배역
3. 마감 상태 또는 남은 기간
4. 연습 및 공연 일정 요약
5. 지원 자격 요약
6. 필요한 제출 자료
7. 공연사명
8. 썸네일

권장 구조:

```text
[마감 D-5] [뮤지컬]
공연명
모집 배역 2개
연습 9.10~10.20 · 공연 11.01~12.15
프로필 · 사진 2장 · 노래 영상
공연사명
```

상태:

- 지원 가능
- 마감 임박
- 마감
- 작성 중인 지원서 있음
- 지원 완료

마감 공고는 삭제하지 않고 버튼을 비활성화하며 `지원이 마감되었어요`를 표시한다.

### 11.8 Role Selector

- 배역명, 성별/연령 조건, 주요 요구사항을 함께 표시한다.
- 하나의 배역만 선택 가능한 경우 radio card를 사용한다.
- 선택된 배역은 blue 보더, 체크 아이콘, 옅은 blue 배경으로 표시한다.
- 조건에 맞지 않는 배역은 숨기지 않고 사유와 함께 비활성화할 수 있다.

### 11.9 File & Media Selector

기존 자료 선택과 신규 등록을 명확하게 분리한다.

구조:

- `저장한 자료에서 선택`
- `새 자료 등록`

자료 카드 표시 정보:

- 썸네일 또는 파일 아이콘
- 자료명
- 유형
- 등록일 또는 최근 수정일
- 사용 목적
- 파일 크기 또는 영상 길이
- 선택 상태

상태:

- unselected
- selected
- uploading
- uploaded
- failed
- invalid format
- outdated warning
- link unavailable

오래된 자료 경고 예시:

`1년 전에 등록한 자료예요. 현재 모습과 다른지 확인해 주세요.`

### 11.10 Progress Stepper

지원서 작성 단계:

`배역 선택 → 기본 정보 → 경력 및 자료 → 추가 질문 → 최종 확인`

- 현재 단계, 완료 단계, 남은 단계를 구분한다.
- 완료된 단계는 다시 이동할 수 있다.
- 모바일에서는 현재 단계명과 `3/5`를 함께 표시한다.
- 오류가 있는 완료 단계에는 경고 아이콘을 표시한다.

### 11.11 Applicant Table

공연사 데스크톱 화면의 핵심 컴포넌트다.

권장 열:

- 선택 checkbox
- 프로필 사진
- 이름
- 지원 배역
- 나이
- 키
- 주요 경력
- 지원일
- 상태
- 메모 여부
- 더보기

규칙:

- 행 높이 64~72px
- 헤더는 sticky 가능
- 선택된 행은 blue-50 배경과 좌측 3px indicator로 표시 가능
- hover와 선택 상태를 구분한다.
- 열 고정이 필요한 경우 이름과 상태를 우선한다.
- 텍스트가 길면 말줄임과 tooltip을 사용한다.
- 정렬 가능한 열은 아이콘과 `aria-sort`를 제공한다.

### 11.12 Applicant Detail Panel

필수 그룹:

- 기본 프로필
- 지원 배역 및 지원 정보
- 주요 경력
- 프로필 사진
- 노래 영상
- 연기 영상
- 공연별 추가 답변
- 내부 평가와 메모
- 상태 변경 이력

상단 고정 정보:

- 이름
- 지원 배역
- 나이와 키
- 현재 상태
- 즐겨찾기 여부

사진과 영상을 확인하는 동안에도 상단 핵심 정보가 사라지지 않게 한다.

### 11.13 Video Player

- 검정 배경을 사용한다.
- 영상 유형, 제목, 길이를 표시한다.
- 여러 영상은 탭 또는 리스트로 전환한다.
- 재생 중에도 지원자 이름과 상태 변경 컨트롤을 유지한다.
- 링크 오류 시 원인을 설명하고 원본 링크 열기 기능을 제공할 수 있다.

### 11.14 Toast

- 성공, 오류, 안내 상태를 지원한다.
- 3~5초 후 사라지되 중요한 오류는 사용자가 닫을 수 있게 한다.
- 행동을 취소할 수 있는 경우 `실행 취소`를 제공한다.

예시:

- `지원서가 임시 저장되었어요.`
- `검토 상태를 ‘보류’로 변경했어요.`
- `파일을 업로드하지 못했어요. 다시 시도해 주세요.`

### 11.15 Dialog & Bottom Sheet

- 데스크톱은 dialog, 모바일은 bottom sheet를 우선한다.
- 단순 정보 확인에는 모달을 사용하지 않는다.
- 위험한 행동에만 확인 과정을 사용한다.

예시:

- 지원서 제출
- 지원 철회
- 공고 삭제
- 상태 일괄 변경

파괴적 행동의 버튼 순서:

- Primary 또는 안전한 행동
- Destructive 행동

### 11.16 Empty State

빈 화면은 현재 상태와 다음 행동을 함께 설명한다.

예시:

- `아직 지원한 공고가 없어요.`
  - `오디션 공고 둘러보기`
- `조건에 맞는 지원자가 없어요.`
  - `필터 초기화`
- `등록한 영상이 없어요.`
  - `영상 등록하기`

### 11.17 Loading State

- 목록은 레이아웃이 유지되는 skeleton을 사용한다.
- 버튼 로딩 중에는 중복 클릭을 막는다.
- 파일 업로드는 진행률과 남은 상태를 표시한다.
- 전체 화면 spinner만 표시하는 방식을 피한다.

---

## 12. Applicant Screen Patterns

### 12.1 오디션 목록

#### 목적

지원자가 지원할 공연을 빠르게 찾고 비교한다.

#### 상단에서 하단 순서

1. 페이지 제목과 짧은 설명
2. 검색창
3. 장르, 지역, 마감 여부 필터
4. 적용된 필터 요약
5. 공고 목록
6. 더 불러오기 또는 페이지네이션

#### Primary Action

목록 화면에는 별도의 Primary 버튼을 두지 않는다. 각 공고 카드의 `공고 보기` 또는 카드 전체 클릭을 사용한다.

### 12.2 공고 상세

#### 상단에서 하단 순서

1. 공연명, 공연사, 상태
2. 모집 배역
3. 마감일 및 주요 일정
4. 지원 자격
5. 제출 자료
6. 상세 설명
7. 공연사 정보
8. 지원 전 확인 사항

#### 데스크톱

우측 sticky 패널에 다음을 요약한다.

- 지원 마감
- 선택한 배역
- 필요한 제출 자료
- `지원하기`

#### 모바일

하단 고정 영역에 `지원하기` 버튼을 배치한다.

### 12.3 지원서 작성

#### 단계

1. 배역 선택
2. 기본 정보
3. 경력 및 자료
4. 추가 질문
5. 최종 확인

#### 공통 요소

- 상단 단계 표시
- 자동 저장 상태
- 이전 단계
- 다음 단계
- 누락된 필수 항목 안내

한 화면에 여러 단계를 모두 펼치지 않는다.

### 12.4 기존 자료 선택

- 저장한 자료를 먼저 보여준다.
- 자료를 선택하면 선택 이유와 제출 대상을 명확히 표시한다.
- 다른 자료로 바꾸는 행동은 Secondary로 제공한다.
- 신규 등록은 별도의 영역으로 분리한다.

### 12.5 최종 확인

그룹별로 제출 내용을 요약한다.

- 배역
- 기본 정보
- 경력
- 사진
- 노래 영상
- 연기 영상
- 추가 답변

각 그룹에 `수정` 액션을 제공한다.

제출 버튼 근처 안내:

- 제출 후 수정 가능 여부
- 제출 취소 가능 여부
- 개인정보 제공 범위

### 12.6 지원 완료

- 성공 상태를 명확히 표시한다.
- 공연명과 배역을 다시 보여준다.
- 지원일과 다음 예상 일정을 표시한다.
- 다음 행동은 `지원 내역 보기`로 제공한다.

---

## 13. Company Screen Patterns

### 13.1 대시보드

#### 목적

담당자가 오늘 확인할 업무와 중요한 변화를 빠르게 파악한다.

표시 정보:

- 진행 중인 공고
- 오늘 새로 들어온 지원자
- 검토하지 않은 지원자
- 마감 임박 공고
- 추가 자료 요청 응답
- 오디션 일정

장식용 그래프는 사용하지 않는다. 실제 의사결정에 필요한 지표만 보여준다.

### 13.2 공연 및 공고 관리

- 공연 단위와 공고 단위를 구분한다.
- 상태는 작성 중, 모집 중, 마감, 종료로 구분한다.
- 공고 등록 과정은 단계형으로 구성할 수 있다.
- 미리보기와 실제 지원자 화면의 차이를 최소화한다.

### 13.3 지원자 목록

상단 도구 영역:

- 공연 선택
- 배역 선택
- 상태 필터
- 조건 필터
- 이름 검색
- 정렬
- 필터 초기화

지원자 선택 시:

- 우측 상세 패널 열기
- 선택된 행 유지
- URL 또는 내부 상태로 선택 상태 보존

### 13.4 다중 선택과 일괄 변경

여러 지원자를 선택하면 기본 필터 바 대신 일괄 작업 바를 표시한다.

표시 정보:

- `3명 선택`
- 상태 변경
- 즐겨찾기
- 메시지 또는 안내
- 선택 해제

일괄 변경 전 영향을 받는 인원을 다시 표시한다.

### 13.5 지원자 상세

- 상단 핵심 프로필은 sticky 처리한다.
- 사진과 영상은 큰 영역에서 확인한다.
- 평가와 내부 메모는 지원자에게 노출되지 않음을 명시한다.
- 상태 변경 시 변경 이력에 담당자와 시간을 기록한다.

---

## 14. Interaction Rules

### 자동 저장

- 입력 후 1~2초의 debounce를 적용할 수 있다.
- 저장 중: `저장 중...`
- 저장 완료: `오전 10:32에 저장됨`
- 저장 실패: `저장하지 못했어요. 다시 시도해 주세요.`

저장 실패 상태에서 페이지를 떠날 경우 경고한다.

### 파일 업로드

- 드래그 앤 드롭과 파일 선택을 모두 지원한다.
- 업로드 전 형식과 용량을 검증한다.
- 진행률을 표시한다.
- 실패한 파일만 다시 업로드할 수 있게 한다.
- 업로드 완료 전 제출 버튼을 활성화하지 않는다.

### 마감 처리

- 마감 시간은 날짜와 시간을 함께 표시한다.
- `D-1`만 표시하지 않고 `8월 10일 오후 6시 마감`을 함께 제공한다.
- 마감 이후 작성 중인 지원서는 읽기 전용으로 전환하고 이유를 설명한다.

### 상태 변경

- 단순 상태 변경은 즉시 적용 후 Toast로 알린다.
- 합격, 불합격, 지원 철회처럼 영향이 큰 변경은 확인 단계를 거친다.
- 변경 후 목록과 상세 패널의 상태를 동시에 갱신한다.

### 오류 복구

오류 메시지는 문제와 다음 행동을 함께 설명한다.

좋은 예:

`영상을 불러오지 못했어요. 링크 공개 범위를 확인하거나 원본 링크를 열어 주세요.`

나쁜 예:

`오류가 발생했습니다.`

---

## 15. Content Guidelines

### 기본 말투

- 친절하고 직접적인 해요체를 사용한다.
- 과도한 감탄, 마케팅 표현, 공연계 은어를 피한다.
- 사용자가 해야 할 행동을 구체적으로 말한다.
- 담당자 업무 화면에서는 짧고 정보 중심적인 문장을 사용한다.

### 제목

좋은 예:

- `지원할 배역을 선택해 주세요`
- `제출할 자료를 확인해 주세요`
- `지원자 128명`
- `검토하지 않은 지원자`

피해야 할 예:

- `새로운 무대로 향해볼까요?`
- `당신의 꿈을 펼쳐보세요!`
- `지원하기 좋은 완벽한 기회`

### 버튼

- 행동의 결과를 직접 표현한다.
- 같은 행동에는 같은 문구를 사용한다.

### 빈 화면

현재 상태 + 다음 행동으로 구성한다.

### 오류

문제 원인 + 해결 방법으로 구성한다.

### 날짜

중요한 일정은 절대 날짜를 우선 표시한다.

예시:

`2026년 8월 10일 오후 6시 마감`

보조적으로 `D-5`를 함께 표시할 수 있다.

---

## 16. Responsive Behavior

### Mobile

지원자의 공고 탐색과 지원서 작성을 최우선으로 한다.

- 테이블을 그대로 축소하지 않는다.
- 카드, 아코디언, bottom sheet로 재구성한다.
- Primary Action은 하단 고정 가능
- 입력창과 버튼은 최소 44px 높이
- 좌우 여백 20px
- 탭과 필터는 가로 스크롤 가능
- 상세 정보는 중요도에 따라 접고 펼친다.

### Tablet

- 공연사 필터와 상세 패널은 접을 수 있게 한다.
- 지원자 목록은 간소화된 테이블 또는 리스트를 사용한다.
- 상세 패널은 40~50% 너비의 overlay panel로 전환할 수 있다.

### Desktop

- 공연사 지원자 비교와 검토를 최우선으로 한다.
- 테이블, 필터 바, 상세 패널을 함께 사용한다.
- 마우스 hover에 의존하지 않고 클릭과 키보드에서도 동일 기능을 제공한다.
- 넓은 화면에서 콘텐츠가 지나치게 늘어나지 않도록 핵심 열 너비를 제한한다.

### Wide Desktop

- 목록과 상세를 동시에 충분히 표시한다.
- 여백만 늘리지 않고 비교 가능한 열을 추가할 수 있다.
- 한 화면에 너무 많은 카드 그리드를 만들지 않는다.

---

## 17. Accessibility

- 모든 입력창에 시각적 label을 제공한다.
- 오류 메시지는 해당 입력과 `aria-describedby`로 연결한다.
- 키보드만으로 검색, 필터, 목록 선택, 상세 패널, 상태 변경을 사용할 수 있어야 한다.
- `focus-visible`은 2px blue outline과 2px offset을 사용한다.
- 모달이 열리면 focus를 내부로 이동하고 닫힐 때 trigger로 되돌린다.
- 상태를 색상만으로 표현하지 않는다.
- 아이콘 버튼에는 접근 가능한 이름을 제공한다.
- 프로필 사진과 공연 이미지는 목적에 맞는 대체 텍스트를 제공한다.
- 장식 이미지는 빈 alt를 사용한다.
- 동영상에는 자막 제공 가능성을 고려한다.
- 최소 터치 영역은 44×44px이다.
- 글래스 표면은 배경과 상관없이 충분한 텍스트 대비를 유지한다.

```css
:focus-visible {
  outline: 2px solid var(--border-focus);
  outline-offset: 2px;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

---

## 18. Do's and Don'ts

### Do

- 화면마다 사용자의 목표를 하나로 정의한다.
- Primary Action을 하나만 강조한다.
- 공고 이미지보다 공연명, 배역, 마감일을 먼저 보여준다.
- 기존 자료 선택과 신규 등록을 분리한다.
- 선택된 자료와 제출될 자료를 명확하게 표시한다.
- 중요한 일정은 날짜와 시간을 함께 표시한다.
- 지원자 목록에서 비교에 필요한 정보를 한 줄에 정렬한다.
- 사진과 영상을 보는 동안 지원자 이름과 상태를 유지한다.
- 글래스모피즘은 헤더, 필터, 플로팅 패널에 제한적으로 사용한다.
- 색상은 시맨틱 토큰을 통해 사용한다.
- 로딩, 빈 화면, 오류, 권한 없음, 마감 상태를 구현한다.
- 모바일과 데스크톱을 각 사용 목적에 맞게 재구성한다.

### Don't

- 토스의 화면과 컴포넌트를 그대로 복제하지 않는다.
- 토스 블루, 금융 데이터 구조, 송금 플로우를 그대로 가져오지 않는다.
- 모든 영역을 글래스 카드로 감싸지 않는다.
- 글래스 위에 글래스를 중첩하지 않는다.
- Primary 버튼을 투명하게 만들어 배경에 묻히게 하지 않는다.
- 모든 텍스트와 아이콘을 파란색으로 만들지 않는다.
- 공연이라는 이유로 커튼, 티켓, 가면, 금색 장식을 남용하지 않는다.
- 지원자 화면과 공연사 화면을 같은 밀도와 구조로 만들지 않는다.
- 데스크톱 테이블을 모바일에서 그대로 축소하지 않는다.
- 상태를 색상만으로 전달하지 않는다.
- 마감된 공고의 지원 버튼을 활성 상태로 두지 않는다.
- 저장 및 제출 여부를 불분명하게 표시하지 않는다.
- 실제 업무와 연결되지 않는 장식용 그래프를 사용하지 않는다.
- 긴 설명을 placeholder에만 넣지 않는다.
- 모든 컨테이너에 그림자를 넣지 않는다.

---

## 19. Codex Implementation Rules

코덱스 또는 다른 코드 생성 도구는 UI 작업 전에 이 문서를 읽고 다음 규칙을 따른다.

1. 기존 기능, 라우트, 데이터 구조를 임의로 제거하지 않는다.
2. 디자인 변경은 정보 구조, 레이아웃, 컴포넌트, 문구, 상태 표현을 중심으로 진행한다.
3. 색상을 컴포넌트 안에 직접 하드코딩하지 않고 CSS 변수 또는 디자인 토큰을 사용한다.
4. 새로운 Primary 버튼을 추가하기 전에 현재 화면의 핵심 행동을 확인한다.
5. 글래스모피즘은 `.glass-surface`, `.glass-surface-strong`, `.glass-surface-dark` 규칙을 우선 사용한다.
6. 모든 카드에 글래스 효과를 적용하지 않는다.
7. 지원자 화면은 모바일 우선, 공연사 화면은 데스크톱 업무 효율 우선으로 구현한다.
8. hover, focus-visible, pressed, disabled, loading, error 상태를 함께 구현한다.
9. 로딩, 데이터 없음, 오류, 마감, 권한 없음 상태를 누락하지 않는다.
10. 상태 변경과 파일 업로드는 성공 및 실패 피드백을 제공한다.
11. 버튼과 입력창의 최소 높이는 44px을 유지한다.
12. 접근성 label, aria 속성, 키보드 조작을 고려한다.
13. 토스의 브랜드 자산, 로고, Tossface, 금융 도메인 컴포넌트는 사용하지 않는다.
14. 새로운 UI 패턴이 이 문서와 충돌하면 사용자의 목표 달성과 실수 예방을 우선한다.
15. 기존 화면을 수정할 때 기능을 새로 만드는 것보다 현재 기능을 더 이해하기 쉽게 만드는 것을 우선한다.

---

## 20. Definition of Done

UI 작업 완료 전에 다음을 확인한다.

### 공통

- [ ] 화면의 목적과 Primary Action이 하나로 정리되어 있다.
- [ ] 제목, 본문, 보조 정보의 위계가 분명하다.
- [ ] 색상을 디자인 토큰으로 사용했다.
- [ ] 글래스 효과를 필요한 영역에만 사용했다.
- [ ] 버튼과 입력 상태가 모두 구현되어 있다.
- [ ] 로딩, 빈 화면, 오류 상태가 있다.
- [ ] 모바일과 데스크톱에서 레이아웃이 각각 적절하다.
- [ ] 키보드와 focus-visible을 지원한다.
- [ ] 상태를 색상만으로 표현하지 않았다.

### 지원자 화면

- [ ] 공연명, 배역, 마감일을 빠르게 확인할 수 있다.
- [ ] 필요한 제출 자료를 지원 전에 알 수 있다.
- [ ] 기존 자료 선택과 신규 등록이 구분된다.
- [ ] 현재 제출될 자료가 분명히 표시된다.
- [ ] 누락된 필수 항목을 제출 전에 발견할 수 있다.
- [ ] 지원 완료와 이후 상태를 확인할 수 있다.

### 공연사 화면

- [ ] 지원자를 공연과 배역별로 필터링할 수 있다.
- [ ] 이름, 배역, 조건, 경력, 상태를 빠르게 비교할 수 있다.
- [ ] 사진과 영상 확인 중에도 핵심 정보가 유지된다.
- [ ] 메모와 평가는 지원자에게 노출되지 않음을 구분한다.
- [ ] 다중 선택과 일괄 상태 변경이 안전하게 동작한다.
- [ ] 상태 변경 이력과 성공·실패 피드백이 제공된다.

---

## 21. Reference Boundary

토스 디자인 시스템에서 참고하는 것은 다음 원칙이다.

- 높은 정보 가독성
- 단일 Primary Action
- 시맨틱 토큰 중심의 색상 사용
- 직접적인 버튼 문구
- 상태별 일관된 컴포넌트
- 사용자를 다음 행동으로 안내하는 오류 메시지
- 불필요한 장식을 줄이는 방식

다음 요소는 예술in에 그대로 적용하지 않는다.

- 토스 브랜드 블루와 브랜드 자산
- 송금, 계좌, 금융 데이터 화면 구조
- Tossface 일러스트
- 앱인토스 내비게이션
- 모바일 단일 컬럼을 전체 서비스에 강제하는 규칙
- 모든 요소에 높은 라운드를 적용하는 방식

예술in의 최종 디자인 판단 기준은 토스와 얼마나 비슷한지가 아니라, **지원자가 정확하게 지원하고 공연사 담당자가 많은 지원자를 지속적으로 검토할 수 있는가**이다.
